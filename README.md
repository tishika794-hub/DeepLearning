# Deep Learning Practice

Welcome to my Deep Learning repository! This repository is dedicated to housing practice implementations, experiments, and exercises related to Deep Learning, starting with Artificial Neural Networks (ANNs).

## Repository Structure

- `practiseANN/`: Directory containing code, notebooks, and models related to Artificial Neural Networks.
- `.gitignore`: Specifying untracked files to be ignored by Git (e.g., virtual environments, caches, local checkpoints).

## Getting Started

### Prerequisites

To run the notebooks and scripts in this repository, you'll need Python installed. It is recommended to use a virtual environment:

```bash
# Create a virtual environment
python -m venv .venv

# Activate the virtual environment (Windows Powershell)
.venv\Scripts\Activate.ps1

# Activate the virtual environment (macOS/Linux)
source .venv/bin/activate
```

### Installation

Install any required dependencies (e.g., TensorFlow, PyTorch, NumPy, Pandas, Matplotlib, Scikit-Learn) within your active virtual environment.

## Topics Covered
1. **Artificial Neural Networks (ANN)**
   - Perceptrons and Multi-Layer Perceptrons (MLPs)
   - Feedforward propagation and Backpropagation
   - Optimization algorithms (SGD, Adam, etc.)
   - Activation functions (ReLU, Sigmoid, Tanh)
   - Regularization techniques (Dropout, L2 normalization)
