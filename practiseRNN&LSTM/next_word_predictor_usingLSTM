import numpy as numpy
import pandas as pd
import matplotlib.pyplot as plt
import tensorflow as tf
from tensorflow.keras.preprocessing.text import Tokenizer
from sklearn.preprocessing import MinMaxScaler
from tensorflow.keras.models import Sequential
from tensorflow.keras.layers import LSTM, Dense, Dropout

from sklearn.metrics import r2_score
from sklearn.metrics import mean_squared_error
from sklearn.metrics import mean_absolute_error 

import os
for dirname, _, filename in os.walk('/kaggle/input'):
    for filename in filenames:
        print(os.path.join(dirname, filename))

data = pd.read_csv('../input/daily-climate-time-series-data/DailyDelhiClimateTrain.csv')
tokenizer = Tokenizer()
tokenizer.fit_on_texts([data]) #apne text data ko num mai convert karenge aur ek number de denge so vo next number predict kar sake
tokenizer.word_index() #isse run karne sai pata chalega koun sa num kis text ko mila
#next ham sentence find karenge hamare data mai kitne sentence hai

for sentence in data.split('/n'):
    tokenized_sentence = tokenizer.texts_to_sequences([sentences])[0] #ye har sentence ko number dega according to above num given to each words
# 0 lagane sai text repeat nhi hoga aur num mai aa jayega
# ab hame esa algo lagana hai ki har num in a list agla num predit kar pye
    for i in range(1,len(tokenized_sentence)): #1st sentence mai agar 3 word haito loop 2 baar chalega
        input_sequences.append(tokenized_sentence[:i+1]) #isse jab ham next number uthayenge to pichle loop mai liya hua number + uska next num
        # zero padding lagana padega kyuki sentences ki size same nhi hai to biggest sentence kai word number ki zero padding laga denge

max_len = max([len(x) for x in input_sequences]) #largest sentence nikala
from tensorflow.keras.preprocessing.sequence import pad_sequences
padded_input_sequences(input_sequences, maxlen = max_len, padding='pre')

x = padded_input_sequences[:,:-1] #sare rows nikalenge bas last column vahi chod denge
y = padded_input_sequences[:,-1] #zero hat jayege aur sare sentence 1 mai aa jayenge
#isse ham as a classification prob solve karenge

from tensorflow.keras.utils import to_categorical
to_categorical(y, num_classes = 283) #hamne length liya ki kitna words hai aur numbering in words ki 1 sai start hai to jitne word hai utna number isse hame y.shape ki column value mil jayegi

#model building using LSTM
#hamare model ka 1st layer embading hota hai ye dense kardega jo better representation hai fir LSTM  and then Dense

model = Sequential()
model.add(Embedding(283, 100, input_length=56)) #283 is total word in data ,input length is words in each sentence
model.add(LSTM(150)) #lstm mai kitne nodes
model.add(LSTM(150))
model.add(LSTM(150))
model.add(Dense(283, activation= 'softmax'))
model.compile(loss='categorical_crossentropy', optimizer='adam', metrics=['accuracy'])

#train model
model.fit(x,y,epochs=100)
 #prediction
text = "mail"
#pehle hame test data ko tokenize , pad,and then predict


